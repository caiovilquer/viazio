# Terminal interativo (prompt JLine) + servidor web na porta 8080:
#   ./mvnw spring-boot:run -Dspring-boot.run.profiles=shell
#   java -jar app.jar --spring.profiles.active=shell
#
# Depois: help | pais-por-codigo --codigo BR | viagem --codigo BR | cambio --moeda USD | quit
spring.shell.interactive.enabled=true
